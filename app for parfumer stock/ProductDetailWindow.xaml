<Window x:Class="PerfumeWarehouseWPF.ProductDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подробная информация" Height="500" Width="550"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding ProductName}" FontSize="20" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Бренд: " FontWeight="SemiBold"/>
            <TextBlock Text="{Binding BrandName}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Содержание спирта: " FontWeight="SemiBold"/>
            <TextBlock Text="{Binding AlcoholPercent}"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="Описание:" FontWeight="SemiBold" Margin="0,10,0,5"/>
        <TextBlock Grid.Row="4" Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="5" Text="Ноты:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <ScrollViewer Grid.Row="6" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Notes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="5">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding NoteName}" FontWeight="SemiBold" Width="120"/>
                                <TextBlock Text="{Binding NoteType}" Foreground="Gray"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="7" Content="Закрыть" Width="100" HorizontalAlignment="Right" Margin="0,15,0,0" Click="CloseButton_Click"/>
    </Grid>
</Window>